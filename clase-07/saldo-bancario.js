//operaciones -> array de números
const operaciones = [15, 10, -5];
//función calcular saldos
//recibe un array de operaciones
// saldo de retiros = numero
// saldo de depósitos = numero
// saldo actual = numero
// MAGIA ???
// retornar los tres saldos = [retiros,depósitos,actual]

function calcularSaldos(operaciones) {}

function imprimirMensaje(nombre, apellido, saldos) {}

const resultado = calcularSaldos(operaciones);
imprimirMensaje("Pablo", "Baleztena", resultado);
